// import { SubscriptionPlan } from "@/types/subscription"; // or wherever your type lives
// import { subscriptionPlans } from "@/types/subscription"; // your static plan array

// export const getPlanById = (id: string): SubscriptionPlan | undefined => {
//   return subscriptionPlans.find(plan => plan.id === id);
// };